import 'package:flutter/widgets.dart';

// TODO(phase2): Implement badge chip.
class BadgeChip extends StatelessWidget {
  const BadgeChip({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
