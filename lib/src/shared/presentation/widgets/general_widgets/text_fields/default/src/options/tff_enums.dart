part of 'imports_options.dart';

enum FieldType {
  withBorder,
  withOutBorder,
}

enum SecureType {
  never,
  toggle,
  always,
}

enum PrefixType {
  none,
  icon,
  text,
}

enum SuffixType {
  none,
  icon,
  text,
}

enum BorderType {
  border,
  disabled,
  enable,
  error,
  focus,
}
