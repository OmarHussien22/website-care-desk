// const Duration kConnectTimeout = Duration(minutes: 10);
// const Duration kReceiveTimeout = Duration(minutes: 10);
// const Duration kSendTimeout = Duration(minutes: 10);

const int kConnectTimeout = 20 * 10000;
const int kReceiveTimeout = 20 * 10000;
const int kSendTimeout = 20 * 10000;
